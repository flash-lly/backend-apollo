import { adminimsController } from './adminims.controller';
import express from 'express';

const adminimsRouter = express.Router();

// router.post("/signup", adminims.signup);

// adminimsRouter.post('/login', adminimsController.login);

export { adminimsRouter };
